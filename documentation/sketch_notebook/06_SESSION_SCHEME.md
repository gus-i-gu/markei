# 06_SESSION_SCHEME.md

> Status: Forward checkpoint
> Authority: Main Chat
> Persistence class: Refreshable planning checkpoint
> Scope: Next-session agenda, expected files, unresolved decisions, and continuity frame

---

# 1. Next Session Focus

The next session should prioritize Sketch Notebook methodology refinement after the Cycle 03 stress test.

Primary objective:

```text
Reduce recovery cost without losing evidence richness.
```

Do not start a new Markei implementation feature cycle unless methodology closure is explicitly deferred.

---

# 2. Topics To Resolve

## 2.1 Main-root file roles

Register and formalize:

- `00_PROJECT_STATE.md` as Global State Canon-Checkpoint;
- `05_SESSION_LOG.md` as ever-growing global observational history;
- `06_SESSION_SCHEME.md` as forward checkpoint.

## 2.2 Main staging surfaces

Decide status of:

- `[M]_STAGE/J_MAIN_STAGE.md`;
- `PROVISORY_[M]_DOUBLE_LAB.MD`.

Current working position:

- `J_MAIN_STAGE.md` is useful as a Main staging draft for end-session criticism and progress appreciation.
- `PROVISORY_[M]_DOUBLE_LAB.MD` is temporary and should not become canon without explicit promotion.

## 2.3 File-growth protocol

Define operational rules for:

- ever-growing files;
- refreshable files;
- derived / resorted files;
- canonical files;
- checkpoints;
- forward checkpoints.

Candidate variables:

```text
SPRINT_READ_LIMIT = 800 lines
REPARTITION_REVIEW_LIMIT = 1300 lines
```

Checkpoint guidance candidate:

```text
Target: 200–300 lines
Review: near 400 lines
Failure: 800 lines
```

## 2.4 Reconciliation protocol

Define:

- vertical reconciliation;
- horizontal reconciliation.

Working distinction:

```text
Vertical reconciliation = consistency through semantic layers and time.
Horizontal reconciliation = consistency across operational, didactic, design, and Main domains.
```

## 2.5 Methodology patch placement

Decide where clauses belong:

- `INDEX.md` for navigation registration;
- `FLUX.md` for routing and file-growth operations;
- `PROMOTION_RULES.md` for semantic classes and reconciliation types;
- `METHOD_GLOSSARY.md` for vocabulary;
- optional future protocol file only if existing methodology files become overloaded.

---

# 3. Expected Files To Inspect

Start with:

```text
INDEX.md
methodology/FLUX.md
methodology/PROMOTION_RULES.md
methodology/METHOD_GLOSSARY.md
00_PROJECT_STATE.md
05_SESSION_LOG.md
06_SESSION_SCHEME.md
[M]_STAGE/J_MAIN_STAGE.md
PROVISORY_[M]_DOUBLE_LAB.MD
```

Consult domain checkpoints only if current project state needs confirmation:

```text
operational/10_OPERATIONAL_STATE.md
didactics/08_CONCEPT_MAP.md
design/09_DESIGN_STATE.md
```

Avoid full reread of large canonical or observational files unless the next task is canon audit or drift investigation.

---

# 4. Deferred Application Work

Pending Markei application work remains:

- manual UI QA;
- invalid analytics date input handling;
- same-day timelapse review;
- old Storage/Shortage/Market page file cleanup decision;
- multi-store analytics validation with richer fixture data;
- mobile-readiness audit continuation.

These should remain deferred until methodology closure is patched or deliberately postponed.

---

# 5. Expected Outputs Of Next Session

A successful next session should leave:

- `INDEX.md` updated to register `06_SESSION_SCHEME.md` and any accepted Main staging route;
- `FLUX.md` updated with file-growth/read-window routing;
- `PROMOTION_RULES.md` updated with global state special class and reconciliation types;
- `METHOD_GLOSSARY.md` updated with new method vocabulary;
- decision recorded for `PROVISORY_[M]_DOUBLE_LAB.MD`;
- a canon consistency audit plan prepared or started.

---

# 6. Current Exit Criteria

Before returning to feature work, the method should answer:

1. What is the official class of `00_PROJECT_STATE.md`?
2. Is `06_SESSION_SCHEME.md` a permanent root file?
3. Is `[M]_STAGE` a permanent Main staging folder?
4. Is `PROVISORY_[M]_DOUBLE_LAB.MD` temporary, promoted, or deleted after use?
5. What file-growth threshold constrains future prompts?
6. How should agents read ever-growing files after threshold?
7. How should checkpoints be compacted when they become too verbose?
8. Where are vertical and horizontal reconciliation defined?

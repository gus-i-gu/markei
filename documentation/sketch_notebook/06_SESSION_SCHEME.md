# 06_SESSION_SCHEME.md

> Version: C11-PH05 permanent absorption / Analytics correction authority 7.0
> Status: Active Forward Checkpoint — final Cycle 11 correction
> Persistence Class: Forward Checkpoint
> Knowledge Class: Main / Prospective
> Authority: Main Chat [M]
> Branch: `grm-guarded-provisioning-20260727`
> PH05 implementation: `426235d8b67ac719e494b53cfb23a6c3b06fb489`
> PH05 permanent-domain head: `74638c33a71eb4bc4e5e16aca49526496d3a3610`
> J section 15: `d6c565edcd0504f382262a3f2c7018e2c63cd47b`
> Analytics correction activation: `0e647e76aa8275bda48cea1e9d08427e3d949134`
> Guarded procedure alignment: `a8ee40f83eac376c100555c2cd6dc2a98660a733`
> Current-state source: `00_PROJECT_STATE.md`

---

<!-- TEMPORAL_MARKER:C11-PH05-ABSORBED-ANALYTICS-CORRECTION-ACTIVE-2026-07-31 -->

# Cycle 11 — Final Analytics Correction and Closure Route

## 1. Status and authority

C11-PH01-R01, C11-PH02, C11-PH03 and PH05 are permanently absorbed by all three
functional domains at their named evidence ceilings.

`C11-ANALYTICS-CORRECTION-R01` is the only active Cycle 11 implementation unit.
Its controlling authority is the synchronized D/E/F append at
`0e647e76aa8275bda48cea1e9d08427e3d949134`.

Later compatible Main-only J, 00/05/06 and G_SCRIPTS commits may precede Codex
when they do not overlap D/E/F, source, tests or G/H/I. The current compatible
documentation chain is J section 15 at `d6c565ed…` plus guarded procedure
alignment at `a8ee40f8…`.

## 2. Product mission

> Correct the implemented Analytics composer and result presentation so the
> learner's selected variables, operation, timeframe and scope remain identical
> across one frozen record and every result/export projection.

Required outcomes:

- compact wide and compact/mobile composer;
- one typed multi-select Variables owner;
- no implicit Quantity, Evidence count, Price or other variable;
- typed blocking of categorical-only and unsupported combinations;
- strict inclusive Initial/Final `dd-mm-yyyy` local calendar dates;
- readable fixed-point display conversion without changing integer aggregation;
- one frozen-record source for Chart, Table, interpretation, CSV and PDF;
- UUID-hidden ordinary evidence presentation with stable internal identity;
- retained Account-local, request-count, zero-write, zero-network and History
  handoff boundaries.

The confirmed defect is raw fixed-point/internal compatibility presentation.
Silent default-Quantity insertion is not evidenced. End-to-end variable integrity
must still be proved by the required regression matrix.

## 3. Materialization and evidence outputs

Codex reads the latest `C11-ANALYTICS-CORRECTION-R01` sections in D/E/F and
replaces G/H/I with correction evidence.

Required evidence includes:

- visible choice → typed draft → immutable record → grouped entries → every
  projection mapping;
- all ten Operational regression cases;
- raw fixed-point and compatibility-key absence from user-facing results;
- UUID-hidden wide and compact Variables presentation;
- strict local-date parsing and inclusive final-day conversion;
- Chart unavailability for incompatible axes without Table/export loss;
- request/write/network counts;
- schema/dependency/platform/provider forbidden-surface audit;
- focused and full serial tests;
- Windows release and Android debug builds;
- residual human wide/compact, keyboard, assistive-technology, locale, launch and
  real-device gates.

## 4. Validation and guarded build/run route

Run from `clients/markei_flutter` using the explicit D-section command set.
The controlling full-suite gate is:

```powershell
flutter test --concurrency=1 --no-pub
```

Human Windows diagnosis already recorded `TestExit=0` with serial expanded
reporting. Bare parallel `flutter test` is not the controlling gate.

At `a8ee40f8…`, Main aligned the same serial/no-pub arguments in:

- `GS-BUILD-02`;
- `GS-FLUTTER-WIN`;
- `GS-FLUTTER-AND`.

Do not suppress Drift warnings or change production database behavior.
`GRM.md` and `I_SCRIPTS.ps1` require no change.

## 5. Stable constraints

The correction must not introduce:

- a second Analytics controller, repository, calculation engine, persistence
  owner or export destination;
- schema, migration, generated-source, dependency or native-platform changes;
- Auth, API, Sync, provider, diagnostics, PH03, Closure or unrelated page work;
- network or database writes from Analytics;
- floating-point aggregation;
- removal of stable internal identity;
- persistent, hosted or synchronized saved analyses;
- prediction, recommendation, ranking, causality or production claims;
- Guide content-sequence changes.

The fixed-point calculation boundary, Account scope, selected-scope cap, History
Purchase-ID handoff, pure export builders and one-write destination behavior
remain.

## 6. Evidence boundary carried forward

PH05 evidence: 278 tests passed, four lab-gated skips, analysis, Windows release
build, Android debug build and diagnostic registry passed.

Still unestablished:

- corrected human wide/compact screenshot fidelity;
- keyboard-only manual acceptance;
- Narrator/TalkBack;
- locale and long-string review;
- Windows launch/manual export;
- Android real-device behavior;
- Guide and Analytics learner comprehension;
- live Auth0, Neon, Render, provider and Sync acceptance;
- production readiness.

No KANBAN transition follows from implementation, tests or builds alone.

## 7. Handoff sequence

```text
PH05 implementation 426235d8
-> J section 14 e6ced7fe
-> three PDR2 commits / twelve permanent files
-> PH05 permanent head 74638c33
-> Analytics correction D/E/F 0e647e76
-> J section 15 d6c565ed
-> G_SCRIPTS serial gate alignment a8ee40f8
-> Main 00/05/06 refresh
-> Codex correction implementation and G/H/I
-> Main G/H/I reconciliation
-> correction-delta PDR2 where permanent truth changed
-> bounded human review
-> final C11 closure
-> C12-PHASE01, with backend scope retained for C12-PHASE02
```

## 8. Entry and stop terminal

```text
CYCLE10=CLOSED_AT_GCM03_DEVELOPMENT_SCOPE
C11=ACTIVE_FINAL_CORRECTION
C11_COMPLETED_THROUGH=PH05_PERMANENT_ABSORPTION
C11_CURRENT_UNIT=C11-ANALYTICS-CORRECTION-R01
CORRECTION_D_E_F_ACTIVATION=0e647e76aa8275bda48cea1e9d08427e3d949134
CORRECTION_CODEX_AUTHORITY=ACTIVE_BOUNDED
SERIAL_FULL_SUITE_GATE=REQUIRED
G_SCRIPTS_SERIAL_GATE_ALIGNMENT=COMPLETE
R07_IMPLEMENTATION_AUTHORIZED=NO
GCM04=DEFERRED_TO_C12_PHASE02
LIVE_SYNC_AUTHORITY=NONE
PROVIDER_MUTATION_AUTHORITY=NONE
```

Stop and return to Main on unexplained branch advancement, source/test overlap
outside the D allowlist, schema/migration/dependency/generated/native change,
GRM/GS mutation by Codex, provider operation, live Sync, a second Analytics
owner, new notebook file, methodology change, or a platform build that cannot be
reported honestly.

---

<!-- TEMPORAL_MARKER:C11-PH03-INVESTIGATION-SUPERSEDED-2026-07-31 -->

# Historical Checkpoint — C11-PH03 Settings, Audit and Closure Investigation

> Superseded by the current PH03-closure/PH04-authority checkpoint above. This
> segment preserves the prior investigation route and authorizes no current
> staging or source work.

## 1. Status and activation

C11-PH01-R01 and C11-PH02 are implemented, validated at the automated/build
ceiling and permanently absorbed by all three functional domains.

C11-PH03 is active for investigation only:

```text
Operational -> DEV_STAGE/A_OPERATIONAL.md
Didactic    -> DEV_STAGE/B_DIDACTIC.md
Design      -> DEV_STAGE/C_DESIGN.md
```

No current D/E/F or Codex source authority exists.

## 2. Product mission

> Make Settings and Audit mandatory, coherent product destinations and retire
> Closure from ordinary navigation only after every current Closure capability
> has an explicit evidence-backed disposition.

The accepted product direction is:

- preserve existing functional Settings behavior;
- make Audit functional;
- replace Closure with Audit in ordinary wide and compact navigation;
- preserve truthful Account scope and local/offline authority;
- retain unavailable, stale and unsupported states honestly;
- separate ordinary product behavior from development/support controls.

The exact capability placement is not yet selected. A/B/C must determine it.

## 3. Required investigation outputs

### Operational

Inventory the current Settings, Audit reservation and Closure implementation,
including:

- visible capabilities and actions;
- local tables, repositories, controllers and request behavior;
- Account/Device scope;
- network or provider dependencies;
- sanitization and sensitive-data exposure;
- status freshness and failure states;
- safe read-only actions versus mutation/recovery controls;
- executable validation and host/human evidence costs.

### Didactic

Define learner-visible meaning and distinctions for:

- Settings versus observed status;
- Audit history versus diagnostics;
- System Health versus recovery control;
- Advanced/Support versus ordinary navigation;
- current, previous, stale, unavailable and development-only evidence;
- responsive, keyboard, assistive-technology and comprehension behavior.

No maturity transition follows from implementation or tests alone.

### Design

Produce the complete capability-disposition and ownership map:

- Audit;
- Settings;
- Advanced/Support;
- development-only;
- retired;
- unresolved for Main/human decision.

Define the application ports, projections, dependency direction, lifecycle
ownership, navigation removal and reversibility required by the smallest
schema-free and provider-free materialization.

## 4. Stable constraints

PH03 must not silently become the former R07 causal/backend implementation.

Explicit non-goals:

- new diagnostic ledger or causal authority;
- schema, migration or generated-source change;
- Sync pipeline instrumentation redesign;
- hosted cross-device Audit;
- provider-side observability or telemetry;
- automatic Retry, Recovery or repair;
- live Sync validation;
- Account, Device, enrollment, Auth0, Neon or Render mutation;
- multiple-user/multiple-Account acceptance;
- GCM04 or production acceptance.

Existing transaction, cursor, acknowledgement, JWT/JWKS, least-privilege,
migration and Cycle 10 acceptance boundaries remain unchanged.

## 5. Evidence boundary carried forward

C11-PH01/PH02 automated tests, analysis and Windows/Android builds passed.
Human screenshots, assistive technology, keyboard-only practical use, locale,
real-device and comprehension acceptance remain not performed.

Those human gates remain visible but do not block PH03 investigation. They must
not be misreported as passed during later materialization.

## 6. Investigation and handoff sequence

```text
Main PH03 activation
-> Operational A investigation
-> Didactic B investigation
-> Design C investigation
-> Main state-of-union and J reconciliation
-> bounded D/E/F, only if evidence supports materialization
-> Codex implementation and G/H/I
-> permanent-domain absorption
-> Main continuity closure
```

The investigations may run independently but must each inspect the same branch
lineage and preserve disagreements. Main owns the final synthesis.

## 7. Entry and stop terminal

```text
CYCLE10=CLOSED_AT_GCM03_DEVELOPMENT_SCOPE
C11=ACTIVE
C11_COMPLETED_THROUGH=C11-PH02
C11_CURRENT_UNIT=C11-PH03-INVESTIGATION
PH03_SCOPE=SETTINGS_AUDIT_CLOSURE_DISPOSITION
PH03_STAGING_AUTHORITY=A_B_C_ONLY
PH03_SOURCE_AUTHORITY=NONE
D_E_F_AUTHORITY=NONE
CODEX_AUTHORITY=NONE
R07_IMPLEMENTATION_AUTHORIZED=NO
GCM04=DEFERRED_TO_C12_PHASE02
LIVE_SYNC_AUTHORITY=NONE
PROVIDER_MUTATION_AUTHORITY=NONE
```

Stop and return to Main if an investigation requires source mutation, a new
notebook file, methodology change, schema/migration, provider operation, live
Sync, or a decision that cannot be resolved within the three domain reports.

---

<!-- TEMPORAL_MARKER:C10-PREPARATION-2026-07-14 -->

# Cycle 10 — Cross-Platform Local Beta Consolidation

## 1. Status

Cycle 09 is closed. Cycle 10 is prepared as the next investigation cycle but has no active
source, schema, dependency or host-mutation authority.

Activation requires:

```text
clean shared branch
→ complete methodology boot
→ fresh A/B/C investigation
→ Main J reconciliation
→ controlling D/E/F
→ bounded Codex materialization
```

Cycle 09 D/E/F are consumed and must not be reused.

## 2. Governing schedule decision

Human direction moves UI polishing and UX improvement to Cycle 11. Cycle 10 must not silently
reintroduce the deferred visual scope as “consolidation.”

Deferred to Cycle 11:

- target-image page recomposition and visual polish;
- full responsive Lists tables/cards/filters and summary presentation;
- page-level adoption/expansion of Markei design components;
- History double-click and Product-detail UX refinement;
- native-share presentation and broader accessibility interaction improvement;
- the proposed minimum Analytics page.

Cycle 10 may record baseline visual/accessibility observations but should not claim their repair.

## 3. Mission

> Consolidate the local Flutter/Drift beta through data-recovery proof, deterministic artifact
> integrity, measured local behavior, behavior-preserving modularity and bounded Windows/Android
> functional evidence, without redesigning the visible product or activating cloud scope.

Cycle 10 is a reliability and evidence cycle, not public-release preparation.

## 4. Starting state

Available from Cycle 09:

- handwritten Drift schema v4 and tested sequential migrations;
- immutable Account-scoped Person `@...`, Payment Method `#...` and Product visible codes;
- optional Purchase references and archive-resolvable labels;
- manual local Purchase occurrence converted to persisted UTC;
- exact Product-code lookup/autofill without implicit Item addition;
- supported dimensional units and same-unit BULK fixed-point calculation;
- transient rebuildable Lists projections;
- selected-Purchase deterministic CSV/PDF generation;
- typed application failures;
- Windows build/bounded launch evidence;
- protected Python/PySide6 isolation.

Carried technical boundaries:

- migration failure/rollback/no-silent-reset evidence is incomplete;
- export destination, cancellation, cleanup and recovery policy are not fully evidenced;
- Lists, lookup, History and export paths lack measurement-driven performance decisions;
- Windows lacks a complete recorded manual functional workflow;
- Android build/install/runtime/lifecycle is host-unvalidated because Java was unavailable;
- Purchase, Catalogue and History exceed the ordinary modularity guideline;
- visual and accessibility convergence is deferred to Cycle 11.

## 5. Proposed workstreams

### A. Baseline and recovery integrity

- freeze the exact schema-v4/database fixture and Cycle 10 acceptance matrix;
- test representative v1/v2/v3→v4 migration, close/reopen and retained data;
- exercise migration failure and verify rollback/no-silent-reset behavior;
- document database location, backup boundary and recovery failure outcomes;
- preserve registered Purchase facts and protected Python storage isolation.

### B. Artifact and local portability integrity

- validate deterministic CSV/PDF content for explicit selected Purchases;
- define destination, cancellation, overwrite, cleanup and privacy behavior as contracts;
- decide whether Cycle 10 proves export only or also local backup/restore/import;
- do not add native sharing UI or any implicit upload path;
- keep exports read-only and user-initiated.

### C. Measurement before optimization

- establish representative local fixtures and timing method;
- measure Lists projection, exact lookup, Catalogue search, History and export;
- add indexes, paging or query changes only when evidence identifies a problem;
- preserve Product-first Lists semantics and rebuildability;
- report fixture size, host and command with every performance claim.

### D. Behavior-preserving modular extraction

- split Purchase, Catalogue and History into bounded page sections/controllers;
- keep current visible behavior, data ownership and navigation unchanged;
- avoid design-system expansion or page recomposition;
- add/retain regression tests before extraction;
- stop if extraction requires an unsettled UX decision.

### E. Cross-platform functional evidence

- rerun Flutter analysis and the full automated suite;
- record a complete Windows Home→Catalogue→Purchase→Lists→History functional workflow;
- restore Java/Gradle only under explicit host authority;
- validate Android build/install/launch and bounded lifecycle behavior when the host permits;
- classify build, emulator, physical-device, lifecycle and manual evidence separately;
- retain visual/accessibility findings as Cycle 11 inputs rather than Cycle 10 closure claims.

### F. Documentation continuity

- keep evidence states narrow and environment-specific;
- update Guide/privacy wording only from accepted local behavior;
- reconcile permanent domain checkpoints before Main closure;
- preserve unchanged learner maturity unless direct learner evidence appears.

## 6. Suggested bounded units

```text
C10-U01  Baseline, fixtures and acceptance matrix
C10-U02  Migration failure, rollback, reopen and local recovery evidence
C10-U03  CSV/PDF artifact integrity and portability-policy decision
C10-U04  Measurement-led local query/performance correction, if evidence requires it
C10-U05  Behavior-preserving Purchase/Catalogue/History extraction
C10-U06  Windows and Android functional validation
C10-U07  Domain promotion and Main-root closure
```

Each unit needs one invariant, rollback boundary and named evidence story. A unit may conclude
with no source change when investigation does not justify mutation.

## 7. Entry decisions for Main/human

1. Does Cycle 10 cover deterministic export only, or local backup/restore/import as well?
2. Which database versions and representative fixtures are mandatory for recovery evidence?
3. What Windows manual workflow is required for functional acceptance?
4. Which Android host/emulator/physical-device checks are realistically available?
5. What fixture sizes and timing thresholds justify query/index work?
6. Should modular extraction precede or follow platform functional validation?

These decisions require investigation; none authorizes implementation by itself.

## 8. Explicit non-goals

- UI aesthetic convergence or target-image reconstruction;
- new UX interaction policy, native-sharing UI or accessibility redesign;
- Analytics or Household behavior;
- authentication, authorization, API/Neon or remote storage;
- upload/download, convergence or multi-device synchronization;
- Product merge/correction, Store redesign or registered Purchase mutation;
- production signing, store publication or public release;
- PySide6 retirement.

Local SyncEvent/PendingEvent preparation is not real synchronization.

## 9. Evidence gates

| Claim | Minimum evidence |
| --- | --- |
| migration preserves data | representative file-backed migration and reopen |
| failure cannot silently reset | induced failure plus rollback/original-file evidence |
| export is deterministic | repeated artifact comparison on named fixtures |
| backup/restore works | explicit round trip and identity/data comparison |
| query is improved | repeatable before/after measurement on the same fixture/host |
| extraction preserves behavior | focused regressions plus unchanged workflow observation |
| Windows workflow works | build and recorded complete manual functional path |
| Android boundary works | build/install/launch and named lifecycle evidence |
| learner maturity changed | explicit learner explanation, prediction or transfer |

No green test suite proves visual parity, accessibility acceptance, production readiness or
cloud synchronization.

## 10. Exit direction

Proposed Cycle 10 exit:

- schema-v4 recovery behavior is evidenced at the agreed fixture/version boundary;
- export/portability scope is truthful and documented;
- optimization decisions are measurement-led;
- selected oversized pages are extracted without intentional visual/UX change;
- Windows functional workflow is recorded;
- Android is evidenced or remains explicitly host-unvalidated with a recovery route;
- permanent domains and Main continuity are reconciled;
- Cycle 11 receives preserved visual/UX observations without premature closure claims.

## 11. Recovery route

At Cycle 10 entry read:

1. `00_PROJECT_STATE.md`;
2. this forward checkpoint;
3. `operational/10_OPERATIONAL_STATE.md` and `04_TODO.md`;
4. `design/09_DESIGN_STATE.md` for schema and modularity boundaries;
5. `didactics/08_CONCEPT_MAP.md` for stable vocabulary and maturity;
6. post-Codex J/G/H/I only for unresolved Cycle 09 evidence;
7. repository source only for the selected first unit.

```text
Cycle 09: closed
Cycle 10 perspective: prepared
A/B/C: not yet staged
D/E/F: not prepared
source/schema/dependency/host authority: inactive
Cycle 11 visual/UX scope: preserved and deferred
```

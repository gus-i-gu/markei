# Markei — Global Project State

> Version: 0.8
> Status: Active
> Persistence Class: Main-root continuity
> Knowledge Class: Global current-state checkpoint
> Authority: Main Chat
> Branch: grm-guarded-provisioning-20260727
> Latest Main reconciliation: d6c565edcd0504f382262a3f2c7018e2c63cd47b, J section 15
> PH05 implementation: 426235d8b67ac719e494b53cfb23a6c3b06fb489
> PH05 permanent-domain head: 74638c33a71eb4bc4e5e16aca49526496d3a3610
> Active Analytics correction authority: 0e647e76aa8275bda48cea1e9d08427e3d949134

---

## 1. Current milestone

Cycle 11 is in its final bounded correction unit.

C11-PH01, PH02, PH03 and PH05 are permanently absorbed at their recorded
implementation, automated/build and human-evidence ceilings. The only active
source authority is C11-ANALYTICS-CORRECTION-R01 in the controlling D/E/F tails
published at 0e647e76.

Cycle 11 is not yet closed. Closure requires corrective Codex materialization,
new G/H/I, bounded human review, final domain reconciliation and a final Main
continuity refresh.

Cycle 10 remains closed at the two-Device, one-Account development boundary.
GCM04, multiple-Account/provider resilience, revocation, outage recovery,
retention/rebootstrap and production lifecycle remain assigned to C12-PHASE02.

## 2. Accepted application state

The active product is the Flutter/Drift local beta. Its ordinary navigation and
local workflows include Home, Purchase, Catalogue, Lists, History, Analytics,
Settings, Audit and Guide; Closure is absent from ordinary navigation.

Accepted C11 behavior includes:

- adaptive visual foundation and core-page composition;
- deterministic Account-scoped local Analytics with session-only saved records;
- functional local Settings and bounded read-only Audit;
- History action selection separate from detail opening;
- typed History Purchase-scope handoff to Analytics;
- one shared export destination boundary;
- two set-based reads for non-empty selected History export;
- one final collision-safe write for each successful explicit export;
- Windows Downloads at automated/build ceiling;
- Android public Downloads as typed unavailable with zero writes;
- local zero-I/O Home and Guide surfaces;
- retained @NNN Person and #NNN Payment Method references.

No C11 phase expanded schema, migrations, hosted truth, prediction, causality,
recommendation, implicit upload, provider control or production acceptance.

## 3. Architecture and execution boundary

Permanent design owns one composition root, one Analytics workspace, one
ExportDestinationPort and one LocalExportDestination. Pure History and Analytics
builders remain separate from filesystem effects. Stable internal identities,
fixed-point calculation, record fingerprints, local Account scope, Drift schema
and provider contracts remain authoritative.

PH05 evidence includes formatting and analysis, 278 passed tests with four
lab-gated skips, Windows release build, Android debug build, diagnostic-registry
check and unchanged schema/dependency/generated/native/provider surfaces.

The later human serial diagnostic also passed the complete suite with
--concurrency=1 --no-pub and TestExit=0. The failed bare-parallel guarded run is
procedure drift, not evidence of an Analytics source regression.

## 4. Active correction and preserved limits

The confirmed Analytics defect is user-facing raw fixed-point values and
internal compatibility keys. Read-only tracing found no evidenced silent
default-Quantity insertion. End-to-end calculation-variable integrity remains a
required regression gate because the existing suite did not assert one frozen
record consistently across Chart, Table, interpretation, CSV and PDF.

The active correction requires a compact typed composer, strict inclusive
dd-mm-yyyy date fields, human-scale results, no ordinary visible compatibility
keys or UUIDs, Date-Time and Store evidence, and parity across every projection
and export while preserving request, write, network, schema and dependency
ceilings.

The Guide remains structurally implemented with eight local anchored sections,
but its learner sequence is partially contradictory: Lists is absent as a
separate teaching boundary and other teaching responsibilities are redistributed.
Human comprehension remains unestablished.

Manual Windows export, Android public-document behavior, full keyboard-only,
Narrator/TalkBack, locale, real-device and learner-comprehension evidence remain
unvalidated. Live Auth0, Neon, Render, provider and Sync operations were not part
of PH05.

## 5. Current authority

Codex authority is active only for the Analytics handwritten source/tests and
replacement G/H/I paths named by D/E/F at 0e647e76. Codex must not change J,
Main-root files, permanent domains, methodology, schema, migrations, generated
code, dependencies or native platform files.

The Main-owned G_SCRIPTS.md correction is now published: GS-BUILD-02,
GS-FLUTTER-WIN and GS-FLUTTER-AND use --concurrency=1 --no-pub. GRM.md and
I_SCRIPTS.ps1 required no identifier or dispatch change. The explicit serial
D/E/F commands remain the controlling Codex validation; guarded GRM procedures
are no longer blocked by the former bare-parallel test command.

## 6. Active risks and deferrals

- Analytics projection integrity awaits the corrective regression matrix and
  bounded human UI review.
- Guide sequence fidelity is unresolved and outside the active Analytics allowlist.
- Android public Downloads remains typed unavailable.
- human accessibility, locale, real-device and comprehension evidence is absent.
- production and multi-Account/provider lifecycle claims remain deferred to C12.
- no passing automated suite proves learner maturity or production acceptance.

## 7. Recovery route

Read in this order:

1. 06_SESSION_SCHEME.md for the active correction and exit gates;
2. J_MAIN_STAGE.md section 15 for global classification and authority;
3. the latest D/E/F Analytics correction sections at 0e647e76;
4. Operational, Didactic and Design checkpoints for permanent depth;
5. replacement G/H/I only after Codex publishes them;
6. source only when the correction or a contradiction requires it.

~~~text
CYCLE=11
PH05_PERMANENT_ABSORPTION=COMPLETE
CURRENT_UNIT=C11-ANALYTICS-CORRECTION-R01
G_SCRIPTS_SERIAL_GATE=CORRECTED
CODEX_AUTHORITY=ACTIVE_BOUNDED
C11_CLOSURE=PENDING_CORRECTION_AND_FINAL_RECONCILIATION
C12_NEXT=AFTER_C11_FINAL_CLOSURE
C12_PHASE02_BACKEND_SCOPE=DEFERRED_AND_PRESERVED
~~~

# Markei — Global Project State

> Version: 0.9
> Status: Active
> Persistence Class: Main-root continuity
> Knowledge Class: Global current-state checkpoint
> Authority: Main Chat
> Branch: grm-guarded-provisioning-20260727
> Latest J reconciliation: 35881bdf076ed1dcfebb0b7cbd35486d3116899e, section 17
> C11 implementation: 17fd65296e960112787b870363b40339f535f5b6
> C11 final permanent-domain head: 9359200464b9218d336850225445fb40ea055763

---

## 1. Current milestone

Cycle 11 is closed at its recorded implementation and automated/build evidence
ceiling. Its final Analytics correction is implemented, validated and
permanently absorbed by Operational, Didactic and Design memory.

All C11 D/E/F and Codex source authority is consumed. No active source,
provider or live-operation packet exists.

Cycle 12 is at entry preparation. Its first bounded work has not yet been
selected. A diagnosed GRM Windows launcher-path defect should be dispositioned
before guarded run evidence is relied upon.

Cycle 10 remains closed at the two-Device, one-Account development boundary.

## 2. Accepted C11 application state

The active product is the Flutter/Drift local beta. Ordinary navigation and
local workflows include Home, Purchase, Catalogue, Lists, History, Analytics,
Settings, Audit and Guide; Closure is absent from ordinary navigation.

Accepted C11 behavior includes:

- adaptive visual foundation and core-page composition;
- deterministic Account-scoped local Analytics with one workspace, one live
  draft and immutable session-only saved records;
- one typed Variables owner with explicit categorical and numeric meaning;
- blocking of categorical-only and unsupported combinations without implicit
  Quantity, count or substitute measure;
- one frozen record controlling Chart, Table, interpretation, CSV and PDF;
- strict inclusive `dd-mm-yyyy` custom dates mapped to the existing UTC
  half-open interval;
- readable quantity, money, unit-price, percentage, signed Difference and
  integer Evidence-count presentation;
- hidden ordinary compatibility keys and UUIDs with stable internal identity
  preserved;
- functional local Settings and bounded read-only Audit;
- History action selection separate from detail opening and typed
  Purchase-scope handoff to Analytics;
- one shared export destination boundary;
- Windows Downloads at automated/build ceiling;
- Android public Downloads as typed unavailable with zero writes;
- local zero-I/O Home and Guide surfaces.

No C11 phase expanded schema, migrations, hosted truth, prediction, causality,
recommendation, implicit upload, provider control or production acceptance.

## 3. Final correction evidence

The confirmed raw fixed-point/internal-key Analytics presentation defect is
corrected at the automated boundary. End-to-end calculation-selection integrity
is automated-validated. Silent default-Quantity insertion and a second
calculation path remain not evidenced.

Passed evidence:

- ten-case Analytics regression matrix;
- focused serial Analytics suite: 24 tests;
- serial full Flutter suite: 286 passed, 4 lab-gated skips;
- formatting and `flutter analyze`;
- Windows release build;
- Android debug build;
- diagnostics-registry drift check.

Analytics effects remain initial local read `1`, Retry `+1`, other actions
`+0`, database writes `0` and network calls `0`. Schema, migrations, generated
source, dependencies, lockfile, native platform files and Auth/API/Sync/provider
contracts remained unchanged.

Existing Drift multiple-database, Windows Boost/CMake and Android Auth0 Kotlin
Gradle compatibility warnings were observed and not suppressed. None is a C11
correction failure.

## 4. Permanent-memory state

The final PDR2 chain from J section 16 contains exactly three commits and twelve
permanent-domain files:

- Didactic `e125b241…`: learner-facing distinctions and unchanged maturity;
- Design `b3185e9d…`: accepted topology, ownership and rollback boundaries;
- Operational `93592004…`: execution evidence, effects and remaining gates.

`KANBAN_TRANSITIONS=NONE`. The domain checkpoints are the first recovery
surfaces for domain-specific detail.

## 5. Evidence ceiling and carried work

C11 closure does not claim:

- human wide/compact screenshot acceptance;
- keyboard-only or Narrator/TalkBack acceptance;
- locale or long-string acceptance;
- Windows application launch or manual Windows Downloads export;
- Android real-device behavior;
- learner comprehension;
- live Auth0, Neon, Render, provider or Sync operation;
- production or multiple-Account acceptance.

The PH05 Guide sequence remains partially contradicted. Android public
Downloads remains typed unavailable. These are carried constraints, not active
C11 source failures.

## 6. GRM Windows launcher diagnosis

The GRM copy command addresses `documentation/I_SCRIPTS.ps1` relative to the
current directory. When invoked from `clients/markei_flutter`, PowerShell looks
for a nonexistent nested `documentation` path and stops before the launcher,
`GS-FLUTTER-WIN` or Flutter runs.

`I_SCRIPTS.ps1` dispatch and the serial `G_SCRIPTS.md` procedure are correct.
The remaining defect is the location-sensitive outer invocation documented in
`GRM.md`. Diagnosis is complete; no solution has yet been authorized or
materialized.

## 7. C12 boundary

C12-PHASE01 retains bounded UI/UX fine tuning and outstanding human acceptance.
C12-PHASE02 retains GCM04, multiple-Account/provider isolation, revocation,
backend resilience, outage recovery, retention/rebootstrap and production
lifecycle.

Main must select the first C12 unit before new A/B/C or D/E/F authority. The
GRM launcher correction is a bounded procedural prerequisite, not permission
to reopen Analytics, Sync or provider source.

## 8. Recovery route

Read in this order:

1. `06_SESSION_SCHEME.md` for C12 entry and stop conditions;
2. `J_MAIN_STAGE.md` section 17 for final C11 classification;
3. Operational, Didactic or Design checkpoint for domain depth;
4. complete G/H/I only when exact C11 materialization evidence is needed;
5. source only when a new bounded C12 task requires implementation truth.

~~~text
CYCLE11=CLOSED_AT_RECORDED_AUTOMATED_BUILD_EVIDENCE_CEILING
C11_ANALYTICS_CORRECTION=PERMANENTLY_ABSORBED
C11_FINAL_DOMAIN_HEAD=9359200464b9218d336850225445fb40ea055763
KANBAN_TRANSITIONS=NONE
C11_SOURCE_AUTHORITY=NONE
GRM_FLUTTER_WIN_LAUNCHER_PATH=DIAGNOSED_UNRESOLVED
C12=ENTRY_PREPARATION
C12_ACTIVE_UNIT=UNSELECTED
C12_PHASE02_BACKEND_SCOPE=DEFERRED_AND_PRESERVED
~~~

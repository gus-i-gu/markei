# G_OPS_CODEX - Cycle 07 Sprint 05 Operational Codex Report

> Status: Implemented, built, launched on Android emulator, and regression-validated
> Branch: `cycle-07-mobile-preparation`
> Starting HEAD: `c2d74e445a6c7819b9c36573f6d63836d990c471`
> Date: 2026-07-12

## Source Stage Files

- `documentation/sketch_notebook/DEV_STAGE/D_OPS_STAGE.md`
- `documentation/sketch_notebook/DEV_STAGE/E_DDC_STAGE.md`
- `documentation/sketch_notebook/DEV_STAGE/F_DSN_STAGE.md`

## Methodology Boot

- Read `AGENTS.md`.
- Read `documentation/sketch_notebook/INDEX.md`.
- Read `documentation/sketch_notebook/methodology/PROMPT_COLLECTION.md`.
- Initialized through PRI-CODEX materialization rules.
- Read current project/session checkpoints and J §23.
- Read Sprint 05 D/E/F completely before editing.

## Preflight And Host State

- Branch verified: `cycle-07-mobile-preparation`.
- `git pull --ff-only`: already up to date at start.
- Starting HEAD verified: `c2d74e445a6c7819b9c36573f6d63836d990c471`.
- H Flutter SDK selected and used: `H:\Users\Gus\develop\flutter`.
- `C:\Users\gusrm\flutter` inspected only; it was not deleted or modified.
- Android SDK installed under `C:\Users\gusrm\AppData\Local\Android\Sdk`.
- Android Studio installed: Google Android Studio 2026.1.1.10.
- Android licenses accepted after human confirmation.
- Visual Studio Build Tools 2022 was already healthy for Windows Flutter builds.
- Existing unrelated/untracked `clients/markei_flutter/devtools_options.yaml` was preserved and not committed.
- Host-local `clients/markei_flutter/android/local.properties` was converged to the H Flutter SDK but remains ignored and uncommitted.

## Writable Scope Used

- `clients/markei_flutter/android/**`
- `clients/markei_flutter/lib/**`
- `clients/markei_flutter/test/**`
- `documentation/sketch_notebook/DEV_STAGE/G_OPS_CODEX.md`
- `documentation/sketch_notebook/DEV_STAGE/H_DDC_CODEX.md`
- `documentation/sketch_notebook/DEV_STAGE/I_DSN_CODEX.md`

## Prohibited Scope Preserved

- No permanent Sketch Notebook files were edited.
- No methodology, A/B/C, D/E/F, J, 00/05/06 files were edited.
- No Python/PySide6 source was changed.
- No Cycle 06 ordinary database was accessed or modified.
- No TypeScript, API, Neon/Postgres, authentication, real sync, release signing, Play Store work, iOS work, legacy import, or PySide6 retirement was implemented.
- Python `__pycache__` artifacts generated by regression execution were removed/reverted and not retained as evidence.

## Source Implemented

- Android application identity changed from `com.example.markei` to `com.gusigu.markei`.
- Android app label changed from `markei` to `Markei`.
- Kotlin `MainActivity` moved to package path `com/gusigu/markei`.
- Android compile/target SDK pinned to 36 for Sprint 05 SDK alignment.
- Flutter startup changed to async composition bootstrap.
- Added `LocalDeviceIdentityRepository` backed by Drift.
- Replaced hard-coded `windows-device` composition identity with database-owned UUID v4 device identity.
- Device bootstrap now creates the local account if needed, creates one UUID v4 device row if none exists, and reuses it after close/reopen.
- Historical non-UUID device rows such as `windows-device` are preserved but not reused for new app bootstrap.
- Purchase page wrapped in `SafeArea` for Android insets.
- Purchase page now displays staged BRL total for the controlled multi-item workflow.
- Added phone-width widget workflow coverage for purchase registration, staged totals, and history.
- Added local device identity tests for fresh creation, close/reopen reuse, distinct fresh databases, monotonic sequence after reopen, and historical non-UUID preservation.

## Host Tools Installed Or Configured

- Downloaded and unpacked Android command-line tools.
- Installed Android SDK components:
  - `platform-tools`
  - `platforms;android-36`
  - `build-tools;36.0.0`
  - `emulator`
  - `cmake;3.22.1`
  - `ndk;28.2.13676358`
  - `system-images;android-36;google_apis;x86_64`
- Created AVD `Markei_Sprint05_API36` using Google APIs x86_64 system image.
- Configured Flutter Android SDK path to `C:\Users\gusrm\AppData\Local\Android\Sdk`.

## Validation Commands And Results

```text
flutter doctor -v
flutter pub get
dart format .
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
flutter build apk --debug
flutter build windows
python -m unittest discover -s tests
```

- `flutter doctor -v`: Android toolchain healthy; Flutter uses `H:\Users\Gus\develop\flutter`; Android SDK 36 detected; licenses accepted. Chrome remains unavailable and outside Sprint 05 scope.
- `dart format --output=none --set-exit-if-changed .`: passed after applying formatter.
- `flutter analyze`: passed with no issues.
- `flutter test`: passed, 27 tests.
- `flutter build apk --debug`: passed; built `build\app\outputs\flutter-apk\app-debug.apk`.
- APK badging evidence: package `com.gusigu.markei`, application label `Markei`, launchable activity `com.gusigu.markei.MainActivity`, compile SDK 36.
- Emulator boot: `Markei_Sprint05_API36` booted as `emulator-5554`.
- APK install: passed with `Success`.
- Android launch: passed; Markei process observed running.
- Android app-private database observed at `/data/data/com.gusigu.markei/files/markei_shared_beta.sqlite`.
- Emulator database evidence after runtime/manual workflow: one UUID v4 device row for `local-account`, `next_sequence = 2`, and `purchases = 1`.
- Human manual runtime evidence: user confirmed Android purchase upsert/registration worked manually on emulator.
- `flutter build windows`: passed; built `build\windows\x64\runner\Release\markei.exe`.
- `python -m unittest discover -s tests`: passed, 5 tests.

## Android Runtime Notes

- Automated ADB text entry was blocked by emulator OS input/stylus overlays and Gboard behavior, not by a Markei build or persistence failure.
- The app itself launched, displayed the Purchase UI, persisted the Drift database, created the UUID device row, and had purchase data present after user manual interaction.
- No app-data clear or database reset was used to manufacture passing evidence.

## Implemented And Tested

- Android application/package identity.
- Android debug build.
- Android emulator launch.
- Database-owned UUID v4 device identity.
- Close/reopen identity reuse.
- Device sequence monotonicity for the persisted identity.
- Preservation of historical `windows-device` rows without reuse.
- Phone-width Purchase/History widget flow.
- Windows build regression.
- Python beta regression.

## Implemented But Host-Limited

- Android lifecycle was partially runtime-observed by launch, force-stop/relaunch, and app-private database inspection.
- Full automated Android UI input was host-limited by emulator input overlays; human manual acceptance supplied workflow confirmation.

## Deferred

- Physical device validation.
- Production signing and Play Store work.
- Android release build.
- iOS build/runtime.
- Cloud synchronization, authentication, networking, and server work.

